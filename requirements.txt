# Dependencies for document processing
python-docx==0.8.11
PyPDF2==3.0.1
python-magic==0.4.27
Pillow==10.0.1

# Optional: for better text extraction
pdfplumber==0.9.0

# Authentication dependencies
bcrypt==4.0.1
# Note: 'secrets' module is part of Python standard library
